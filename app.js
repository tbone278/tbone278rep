let airports=[];const statusDiv=document.getElementById('status');const result=document.getElementById('result');fetch('./airports.json').then(r=>r.json()).then(data=>{airports=data;statusDiv.textContent='Loaded '+airports.length.toLocaleString()+' airports';});function score(a,q){const i=(a.iata||'').toLowerCase();const ic=(a.icao||'').toLowerCase();const city=(a.city||'').toLowerCase();const name=(a.name||'').toLowerCase();if(i===q)return 100;if(ic===q)return 90;if(i.startsWith(q))return 80;if(ic.startsWith(q))return 70;if(city.startsWith(q))return 60;if(name.startsWith(q))return 50;if(city.includes(q))return 40;if(name.includes(q))return 30;return 0;}document.getElementById('search').addEventListener('input',e=>{const q=e.target.value.trim().toLowerCase();if(q.length<2){result.innerHTML='';return;}const matches=airports.map(a=>({a,s:score(a,q)})).filter(x=>x.s>0).sort((a,b)=>b.s-a.s).slice(0,20);result.innerHTML=matches.map(x=>{const a=x.a;return `<div class="card"><div class="iata">${a.iata}</div><div>${(a.city||'-').toUpperCase()} | ${a.country}</div><br><strong>${a.name}</strong><br><br>IATA: ${a.iata}<br>ICAO: ${a.icao||'-'}<br>TIMEZONE: pending timezone dataset<br><br><a target="_blank" href="https://maps.apple.com/?ll=${a.lat},${a.lon}">OPEN IN APPLE MAPS</a></div>`;}).join('');});if('serviceWorker' in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js'));}